### scene

Content variant 1:
- `events`
- `assets`
- `<class 'bs4.element.NavigableString'>`

Attribute variant 1:
- `id` (`s_albatros1`)
- `xsi:schemaLocation` (`http://datenighto.com/schemas/htmlvn/0.1 ./htmlvn.xsd`)
- `xmlns` (`http://datenighto.com/schemas/htmlvn/0.1`)
- `xmlns:xsi` (`http://www.w3.org/2001/XMLSchema-instance`)

### assets

Content variant 1:
- `TextAsset`
- `<class 'bs4.element.NavigableString'>`

Content variant 2:
- (Empty)

Attribute variant 1:
- (Empty)

### TextAsset

Content variant 1:
- `value`
- `<class 'bs4.element.NavigableString'>`

Attribute variant 1:
- `key` (`t_salbatros134.00`)

### value

Content variant 1:
- `<class 'bs4.element.NavigableString'>`

Content variant 2:
- (Empty)

Attribute variant 1:
- `key` (`en`)

### events


Attribute variant 1:
- (Empty)

### ParallelEvent

Content variant 1:
- `events`
- `<class 'bs4.element.NavigableString'>`

Attribute variant 1:
- `name` (`_0`) (optional)

Attribute variant 2:
- `allowInterrupt` (`false`) (optional)
- `auto` (`true`) (optional)
- `name` (`_2V`) (optional)

Attribute variant 3:
- `delay` (`2`) (optional)

Attribute variant 4:
- `allowInterrupt` (`false`) (optional)
- `name` (`_3A`) (optional)

Attribute variant 5:
- `auto` (`true`) (optional)
- `name` (`initialize`) (optional)

Attribute variant 6:
- `auto` (`true`) (optional)
- `delay` (`1`) (optional)
- `name` (`_k`) (optional)

Attribute variant 7:
- `allowInterrupt` (`false`) (optional)
- `delay` (`1`) (optional)
- `name` (`_1c`) (optional)

### AudioCreate

Content variant 1:
- (Empty)

Attribute variant 1:
- `audioId` (`bgm`)
- `layer` (`bgm`)
- `loop` (`true`)
- `play` (`true`) (optional)
- `sound` (`@a_bgm_aroundtown`)

Attribute variant 2:
- `audioId` (`pacman`)
- `auto` (`true`) (optional)
- `layer` (`sfx`)
- `loop` (`true`)
- `name` (`_2U`) (optional)
- `play` (`true`) (optional)
- `sound` (`@a_sfx_pacman`)

Attribute variant 3:
- `audioId` (`firealarm`)
- `auto` (`true`) (optional)
- `delay` (`1`) (optional)
- `layer` (`sfx`)
- `loop` (`true`)
- `play` (`true`) (optional)
- `sound` (`@a_sfx_firealarm`)

Attribute variant 4:
- `audioId` (`bgm`)
- `layer` (`bgm`)
- `loop` (`true`)
- `sound` (`@a_bgm_aroundtown`)
- `volume` (`0`) (optional)

Attribute variant 5:
- `audioId` (`bgm`)
- `delay` (`0.25`) (optional)
- `layer` (`bgm`)
- `loop` (`true`)
- `play` (`true`) (optional)
- `sound` (`@a_bgm_bosstheme`)
- `volume` (`1`) (optional)

Attribute variant 6:
- `allowInterrupt` (`false`) (optional)
- `audioId` (`river`)
- `auto` (`true`) (optional)
- `layer` (`sfx`)
- `loop` (`true`)
- `play` (`true`) (optional)
- `sound` (`@a_sfx_rushingwater`)

Attribute variant 7:
- `audioId` (`crowd`)
- `auto` (`true`) (optional)
- `layer` (`sfx`)
- `loop` (`true`)
- `play` (`true`) (optional)
- `sound` (`@a_sfx_arenacrowd`)

Attribute variant 8:
- `audioId` (`saw`)
- `auto` (`true`) (optional)
- `layer` (`sfx`)
- `loop` (`true`)
- `play` (`true`) (optional)
- `sound` (`@a_sfx_circularsaw`)
- `volume` (`0.2`) (optional)

### TextEvent

Content variant 1:
- (Empty)

Attribute variant 1:
- `char` (`@t_ch_cousin`) (optional)
- `text` (`@t_salbatros134.00`) (optional)

Attribute variant 2:
- `append` (`true`) (optional)
- `char` (`@t_ch_cousin`) (optional)
- `text` (`@t_salbatros136.01`) (optional)

Attribute variant 3:
- `char` (`@t_ch_digdug`) (optional)
- `text` (`@t_salbatros3157.00`) (optional)
- `transform` (`uppercase`) (optional)

Attribute variant 4:
- `append` (`true`) (optional)
- `char` (`@t_ch_davesprite`) (optional)
- `text` (`@t_santibravo134.01`) (optional)
- `transform` (`davesprite`) (optional)

Attribute variant 5:
- `char` (`@t_ch_davesprite`) (optional)
- `letterClass` (`letterSnap`) (optional)
- `letterDelay` (`0.06`) (optional)
- `text` (`@t_sdavesprite1169.00`) (optional)
- `transform` (`davesprite`) (optional)

Attribute variant 6:
- `char` (``) (optional)
- `delay` (`8`) (optional)
- `text` (``) (optional)

Attribute variant 7:
- `char` (``) (optional)
- `name` (`_5`) (optional)
- `text` (`@t_smrdriller2_splash0`) (optional)

Attribute variant 8:
- `auto` (`true`) (optional)
- `char` (``) (optional)
- `name` (`_6`) (optional)
- `text` (``) (optional)

Attribute variant 9:
- (Empty)

Attribute variant 10:
- `delay` (`1.1`) (optional)
- `text` (`*SLAP*`) (optional)

Attribute variant 11:
- `append` (`true`) (optional)
- `auto` (`true`) (optional)
- `char` (`@t_ch_cousin`) (optional)
- `text` (`@t_sintro10.04`) (optional)

Attribute variant 12:
- `append` (`true`) (optional)
- `char` (`@t_ch_cousin`) (optional)
- `letterDelay` (`0.0075`) (optional)
- `text` (`@t_sintro10.06`) (optional)

Attribute variant 13:
- `char` (`@t_ch_taira`) (optional)
- `letterDelay` (`0.0075`) (optional)
- `text` (`@t_staira127.00`) (optional)

### ActorCreate

Content variant 1:
- `styles`
- `<class 'bs4.element.NavigableString'>`

Content variant 2:
- `transitions`
- `styles`
- `<class 'bs4.element.NavigableString'>`

Attribute variant 1:
- `actorId` (`bg`)
- `actorType` (`background`)

Attribute variant 2:
- `actorId` (`trueEnd`)
- `actorType` (`event`)
- `allowInterrupt` (`false`) (optional)
- `duration` (`1`) (optional)
- `name` (`_8`) (optional)

Attribute variant 3:
- `actorId` (`mc`)
- `actorType` (`character`)
- `auto` (`true`) (optional)
- `name` (`_7`) (optional)

Attribute variant 4:
- `actorId` (`albatros`)
- `actorType` (`character`)
- `duration` (`0.5`) (optional)

Attribute variant 5:
- `actorId` (`event`)
- `actorType` (`event`)
- `auto` (`true`) (optional)
- `duration` (`1`) (optional)
- `name` (`_n`) (optional)

Attribute variant 6:
- `actorId` (`bike`)
- `actorType` (`character`)
- `name` (`_0`) (optional)

Attribute variant 7:
- `actorId` (`mc`)
- `actorType` (`character`)
- `auto` (`true`) (optional)
- `duration` (`0.5`) (optional)

Attribute variant 8:
- `actorId` (`pacman`)
- `actorType` (`character`)
- `auto` (`true`) (optional)

Attribute variant 9:
- `actorId` (`pacman`)
- `actorType` (`character`)
- `duration` (`5`) (optional)
- `ease` (`ease-out`) (optional)

### styles

Content variant 1:
- `image`
- `<class 'bs4.element.NavigableString'>`
- `depth`

Content variant 2:
- `image`
- `depth`
- `<class 'bs4.element.NavigableString'>`
- `opacity`

Content variant 3:
- `depth`
- `<class 'bs4.element.NavigableString'>`
- `image`
- `x`
- `opacity`

Content variant 4:
- `image`
- `depth`
- `<class 'bs4.element.NavigableString'>`
- `x`

Content variant 5:
- `image`
- `<class 'bs4.element.NavigableString'>`
- `x`

Content variant 6:
- `depth`
- `<class 'bs4.element.NavigableString'>`
- `scaleX`
- `scaleY`
- `y`
- `image`
- `x`
- `opacity`

Content variant 7:
- `depth`
- `<class 'bs4.element.NavigableString'>`
- `scaleY`
- `scaleX`
- `y`
- `image`
- `x`

Content variant 8:
- `depth`
- `<class 'bs4.element.NavigableString'>`
- `y`
- `image`
- `x`
- `opacity`

Content variant 9:
- `<class 'bs4.element.NavigableString'>`
- `x`

Content variant 10:
- `image`
- `x`
- `<class 'bs4.element.NavigableString'>`
- `opacity`

Content variant 11:
- `depth`
- `<class 'bs4.element.NavigableString'>`
- `scaleX`
- `image`
- `x`
- `opacity`

Content variant 12:
- `depth`
- `<class 'bs4.element.NavigableString'>`
- `scaleX`
- `scaleY`
- `image`
- `x`
- `opacity`

Content variant 13:
- `depth`
- `<class 'bs4.element.NavigableString'>`
- `scaleX`
- `image`
- `x`

Content variant 14:
- `depth`
- `<class 'bs4.element.NavigableString'>`
- `scaleX`
- `y`
- `image`
- `x`

Content variant 15:
- `image`
- `y`
- `<class 'bs4.element.NavigableString'>`
- `x`

Content variant 16:
- `<class 'bs4.element.NavigableString'>`
- `scaleX`
- `image`
- `x`
- `opacity`

Content variant 17:
- `id`
- `<class 'bs4.element.NavigableString'>`
- `image`
- `x`
- `opacity`

Content variant 18:
- `image`
- `id`
- `<class 'bs4.element.NavigableString'>`
- `opacity`

Content variant 19:
- `opacity`
- `<class 'bs4.element.NavigableString'>`
- `x`

Content variant 20:
- `image`
- `scaleX`
- `<class 'bs4.element.NavigableString'>`
- `x`

Content variant 21:
- `depth`
- `<class 'bs4.element.NavigableString'>`
- `y`
- `image`
- `x`

Content variant 22:
- `depth`
- `<class 'bs4.element.NavigableString'>`

Content variant 23:
- `depth`
- `<class 'bs4.element.NavigableString'>`
- `scaleY`
- `scaleX`
- `x`
- `opacity`

Attribute variant 1:
- (Empty)

### image

Content variant 1:
- `<class 'bs4.element.NavigableString'>`

Attribute variant 1:
- (Empty)

### depth

Content variant 1:
- `<class 'bs4.element.NavigableString'>`

Attribute variant 1:
- (Empty)

### opacity

Content variant 1:
- `<class 'bs4.element.NavigableString'>`

Attribute variant 1:
- (Empty)

### x

Content variant 1:
- `<class 'bs4.element.NavigableString'>`

Attribute variant 1:
- (Empty)

### ActorImage

Content variant 1:
- (Empty)

Attribute variant 1:
- `image` (`@i_cousin_default_surprised`)
- `target` (`cousin`)

Attribute variant 2:
- `auto` (`true`) (optional)
- `image` (`@i_cousin_default_neutral`)
- `target` (`cousin`)

Attribute variant 3:
- `image` (`@i_albatross_fistinhand_smirk`)
- `name` (`_0`) (optional)
- `target` (`albatros`)

Attribute variant 4:
- `auto` (`true`) (optional)
- `image` (`@i_albatross_fistinhand_smirk_wink`)
- `name` (`_1b`) (optional)
- `target` (`albatros`)

Attribute variant 5:
- `delay` (`3`) (optional)
- `image` (`@i_cousin_default_mortified`)
- `target` (`cousin`)

Attribute variant 6:
- `auto` (`true`) (optional)
- `delay` (`.5`) (optional)
- `image` (`@i_donko_default_crying_comic`)
- `target` (`donko`)

### FadeEvent

Content variant 1:
- (Empty)

Attribute variant 1:
- `duration` (`.25`)
- `target` (`albatros`)
- `to` (`1`)

Attribute variant 2:
- `auto` (`true`) (optional)
- `duration` (`.33`)
- `name` (`_1O`) (optional)
- `target` (`connie`)
- `to` (`1`)

Attribute variant 3:
- `delay` (`.25`) (optional)
- `duration` (`.25`)
- `target` (`tomari`)
- `to` (`1`)

Attribute variant 4:
- `auto` (`true`) (optional)
- `duration` (`.33`)
- `target` (`digdug`)
- `to` (`1`)

Attribute variant 5:
- `allowInterrupt` (`false`) (optional)
- `auto` (`true`) (optional)
- `duration` (`1`)
- `name` (`_1T`) (optional)
- `target` (`cousin`)
- `to` (`0`)

Attribute variant 6:
- `duration` (`.5`)
- `name` (`_26`) (optional)
- `target` (`antibravo`)
- `to` (`0`)

### ActorCrossfade

Content variant 1:
- (Empty)

Attribute variant 1:
- `duration` (`0.333`)
- `image` (`@i_cousin_default_grin_blush`)
- `opacity` (`1`) (optional)
- `target` (`cousin`)

Attribute variant 2:
- `auto` (`true`) (optional)
- `duration` (`.7`)
- `image` (`@i_bg_vault_closed`)
- `target` (`bg`)

Attribute variant 3:
- `auto` (`true`) (optional)
- `duration` (`.7`)
- `image` (`@i_galaga_wig`)
- `name` (`_1G`) (optional)
- `target` (`galaga`)

Attribute variant 4:
- `duration` (`1`)
- `image` (`@i_bg_dayride`)
- `target` (`bg`)

### NHSceneChange

Content variant 1:
- (Empty)

Attribute variant 1:
- `name` (`_2I`)
- `scene` (`s_day2`)

Attribute variant 2:
- `destroyAudio` (`false`) (optional)
- `musicFadeTarget` (`1`) (optional)
- `name` (`_2`)
- `scene` (`s_albatros6`)

### ActorMove

Content variant 1:
- (Empty)

Attribute variant 1:
- `duration` (`.5`) (optional)
- `target` (`albatros`)
- `x` (`20%`) (optional)

Attribute variant 2:
- `auto` (`true`) (optional)
- `name` (`_1`) (optional)
- `target` (`albatros`)
- `x` (`20%`) (optional)

Attribute variant 3:
- `auto` (`true`) (optional)
- `duration` (`0.25`) (optional)
- `target` (`cousin`)
- `x` (`-28%`) (optional)

Attribute variant 4:
- `auto` (`true`) (optional)
- `duration` (`2`) (optional)
- `name` (`_1c`) (optional)
- `target` (`albatros`)
- `x` (`56%`) (optional)

Attribute variant 5:
- `duration` (`5`) (optional)
- `ease` (`ease-out`) (optional)
- `target` (`pacman`)
- `x` (`30%`) (optional)

Attribute variant 6:
- `duration` (`0.333`) (optional)
- `target` (`miller`)
- `y` (`0%`) (optional)

Attribute variant 7:
- `auto` (`true`) (optional)
- `duration` (`.3`) (optional)
- `name` (`_1X`) (optional)
- `target` (`miller`)
- `y` (`100%`) (optional)

Attribute variant 8:
- `auto` (`true`) (optional)
- `delay` (`.33`) (optional)
- `duration` (`.33 `) (optional)
- `ease` (`ease-in-out`) (optional)
- `target` (`aki`)
- `x` (`-15%`) (optional)

Attribute variant 9:
- `target` (`king`)
- `x` (`35%`) (optional)

Attribute variant 10:
- `delay` (`.33`) (optional)
- `duration` (`3`) (optional)
- `target` (`pacman`)
- `x` (`38%`) (optional)

Attribute variant 11:
- `delay` (`2`) (optional)
- `duration` (`.33`) (optional)
- `ease` (`ease-in-out`) (optional)
- `target` (`cousin`)
- `x` (`-25%`) (optional)

Attribute variant 12:
- `auto` (`true`) (optional)
- `target` (`antibravo`)
- `x` (`20%`) (optional)

Attribute variant 13:
- `allowInterrupt` (`false`) (optional)
- `duration` (`1`) (optional)
- `ease` (`ease-in-out`) (optional)
- `name` (`_A`) (optional)
- `target` (`cousin`)
- `x` (`-80%`) (optional)

Attribute variant 14:
- `allowInterrupt` (`false`) (optional)
- `delay` (`.5`) (optional)
- `duration` (`1`) (optional)
- `ease` (`ease-in-out`) (optional)
- `target` (`students`)
- `x` (`0%`) (optional)

Attribute variant 15:
- `delay` (`.33`) (optional)
- `duration` (`.5`) (optional)
- `target` (`bluemax`)
- `y` (`50%`) (optional)

Attribute variant 16:
- `duration` (`.2`) (optional)
- `target` (`cousin`)
- `x` (`0%`) (optional)
- `y` (`0%`) (optional)

Attribute variant 17:
- `delay` (`0.65`) (optional)
- `target` (`davesprite`)
- `x` (`15%`) (optional)

Attribute variant 18:
- `name` (`_1g`) (optional)
- `target` (`digdug`)
- `x` (`15%`) (optional)

Attribute variant 19:
- `auto` (`true`) (optional)
- `duration` (`.33`) (optional)
- `ease` (`ease-out`) (optional)
- `name` (`_S`) (optional)
- `target` (`miller`)
- `y` (`0`) (optional)

Attribute variant 20:
- `duration` (`0.10`) (optional)
- `name` (`_A`) (optional)
- `target` (`cousin`)
- `x` (`-25%`) (optional)

Attribute variant 21:
- `allowInterrupt` (`false`) (optional)
- `duration` (`1.5`) (optional)
- `name` (`_6`) (optional)
- `target` (`albatros`)
- `x` (`70%`) (optional)

Attribute variant 22:
- `target` (`miller`)
- `y` (`70%`) (optional)

Attribute variant 23:
- `duration` (`.75`) (optional)
- `ease` (`ease-out`) (optional)
- `target` (`miller`)
- `y` (`50%`) (optional)

Attribute variant 24:
- `allowInterrupt` (`false`) (optional)
- `auto` (`true`) (optional)
- `duration` (`3`) (optional)
- `name` (`_O`) (optional)
- `target` (`pacman`)
- `x` (`33%`) (optional)

Attribute variant 25:
- `auto` (`true`) (optional)
- `delay` (`.7`) (optional)
- `duration` (`3`) (optional)
- `target` (`pacman`)
- `x` (`88%`) (optional)

Attribute variant 26:
- `delay` (`0.25`) (optional)
- `target` (`digdug`)
- `y` (`-25%`) (optional)

Attribute variant 27:
- `auto` (`true`) (optional)
- `duration` (`.33`) (optional)
- `ease` (`ease-in-out`) (optional)
- `target` (`meowkie`)
- `x` (`25%`) (optional)

Attribute variant 28:
- `auto` (`true`) (optional)
- `duration` (`0.5`) (optional)
- `ease` (`ease-in`) (optional)
- `name` (`_23`) (optional)
- `target` (`mc`)
- `x` (`100%`) (optional)

Attribute variant 29:
- `auto` (`true`) (optional)
- `delay` (`.33`) (optional)
- `duration` (`.33 `) (optional)
- `ease` (`ease-in-out`) (optional)
- `name` (`_36`) (optional)
- `target` (`mrdriller`)
- `x` (`25%`) (optional)

Attribute variant 30:
- `allowInterrupt` (`false`) (optional)
- `auto` (`true`) (optional)
- `duration` (`1`) (optional)
- `ease` (`ease-in`) (optional)
- `name` (`_2c`) (optional)
- `target` (`galaga`)
- `y` (`-100%`) (optional)

Attribute variant 31:
- `auto` (`true`) (optional)
- `target` (`cousin`)
- `x` (`40%`) (optional)
- `y` (`25%`) (optional)

Attribute variant 32:
- `target` (`nidia`)
- `x` (`20%`) (optional)
- `y` (`0%`) (optional)

### ActorFlip

Content variant 1:
- (Empty)

Attribute variant 1:
- `duration` (`.2`) (optional)
- `target` (`albatros`)

Attribute variant 2:
- `target` (`connie`)

Attribute variant 3:
- `auto` (`true`) (optional)
- `name` (`_t`) (optional)
- `target` (`valkyrie`)

Attribute variant 4:
- `delay` (`1.75`) (optional)
- `target` (`davesprite`)

Attribute variant 5:
- `delay` (`1`) (optional)
- `duration` (`.3`) (optional)
- `target` (`donko`)

Attribute variant 6:
- `auto` (`true`) (optional)
- `duration` (`.2`) (optional)
- `name` (`_P`) (optional)
- `target` (`antibravo`)

Attribute variant 7:
- `duration` (`.3`) (optional)
- `ease` (`ease-in-out`) (optional)
- `target` (`digdug`)

Attribute variant 8:
- `name` (`_0`) (optional)
- `target` (`tomari`)

### SettingChange

Content variant 1:
- `events`
- `<class 'bs4.element.NavigableString'>`

Content variant 2:
- (Empty)

Attribute variant 1:
- `bgActor` (`bg`) (optional)
- `bgImage` (`@i_bg_cafe`)
- `curtainActor` (`curtain`) (optional)
- `curtainFadeTime` (`1`)
- `name` (`_1C`)

Attribute variant 2:
- `bgImage` (`@i_bg_cliff_day`)
- `curtainActor` (`white_swatch`) (optional)
- `curtainFadeTime` (`.5`)
- `name` (`_Q`)

Attribute variant 3:
- `bgImage` (`@i_bg_classroom_a`)
- `curtainFadeTime` (`1`)
- `name` (`_6`)

Attribute variant 4:
- `auto` (`true`) (optional)
- `bgImage` (`@i_bg_stage_cardboard`)
- `curtainFadeTime` (`1`)
- `name` (`_W`)

### AudioDestroy

Content variant 1:
- (Empty)

Attribute variant 1:
- `name` (`_2W`) (optional)
- `target` (`pacman`)

Attribute variant 2:
- `target` (`firealarm`)

### VarEnsure

Content variant 1:
- (Empty)

Attribute variant 1:
- `storeName` (`slot`)
- `value` (`0`)
- `varName` (`pick_count_albatros`)

### y

Content variant 1:
- `<class 'bs4.element.NavigableString'>`

Attribute variant 1:
- (Empty)

### scaleX

Content variant 1:
- `<class 'bs4.element.NavigableString'>`

Attribute variant 1:
- (Empty)

### scaleY

Content variant 1:
- `<class 'bs4.element.NavigableString'>`

Attribute variant 1:
- (Empty)

### IfLTE

Content variant 1:
- `events`
- `<class 'bs4.element.NavigableString'>`

Attribute variant 1:
- `auto` (`true`)
- `lvalue` (`${slot:pick_count_albatros}`)
- `name` (`_1`)
- `rvalue` (`1`)

### BattleMacro

Content variant 1:
- `events`
- `<class 'bs4.element.NavigableString'>`

Content variant 2:
- (Empty)

Attribute variant 1:
- `name` (`_2`)
- `partnerActor` (`albatros`) (optional)

Attribute variant 2:
- `name` (`_1`)

### ActorDestroy

Content variant 1:
- (Empty)

Attribute variant 1:
- `name` (`_0`) (optional)
- `target` (`robotarmy`)

Attribute variant 2:
- `target` (`school`)

### ActorEvent

Content variant 1:
- `styles`
- `<class 'bs4.element.NavigableString'>`

Content variant 2:
- `transitions`
- `styles`
- `<class 'bs4.element.NavigableString'>`

Content variant 3:
- `transitions`
- `<class 'bs4.element.NavigableString'>`

Attribute variant 1:
- `name` (`_1`) (optional)
- `target` (`bg`)

Attribute variant 2:
- `target` (`antibravo`)

Attribute variant 3:
- `duration` (`0.5`) (optional)
- `target` (`cousin`)

Attribute variant 4:
- `duration` (`5`) (optional)
- `ease` (`ease`) (optional)
- `target` (`pacman`)

### BadEndMacro

Content variant 1:
- (Empty)

Attribute variant 1:
- `auto` (`true`)
- `name` (`_3`)
- `varName` (`${slot:pick_count_albatros}`)

### ActorFade

Content variant 1:
- (Empty)

Attribute variant 1:
- `auto` (`true`) (optional)
- `duration` (`0.5`) (optional)
- `name` (`_4`) (optional)
- `opacity` (`0`)
- `target` (`curtain`)

Attribute variant 2:
- `allowInterrupt` (`false`) (optional)
- `auto` (`true`) (optional)
- `duration` (`1`) (optional)
- `name` (`_2`) (optional)
- `opacity` (`1`)
- `target` (`curtain`)

Attribute variant 3:
- `duration` (`1`) (optional)
- `name` (`_2`) (optional)
- `opacity` (`0`)
- `target` (`aki`)

Attribute variant 4:
- `name` (`_3`) (optional)
- `opacity` (`1`)
- `target` (`amazona`)

Attribute variant 5:
- `duration` (`.33`) (optional)
- `opacity` (`1`)
- `target` (`taira`)

Attribute variant 6:
- `delay` (`.2`) (optional)
- `duration` (`.2`) (optional)
- `opacity` (`1`)
- `target` (`students`)

Attribute variant 7:
- `delay` (`0.15`) (optional)
- `opacity` (`0`)
- `target` (`davesprite`)

Attribute variant 8:
- `opacity` (`1`)
- `target` (`pacman`)

Attribute variant 9:
- `allowInterrupt` (`false`) (optional)
- `duration` (`1`) (optional)
- `opacity` (`1`)
- `target` (`curtain`)

Attribute variant 10:
- `auto` (`true`) (optional)
- `duration` (`0.5`) (optional)
- `opacity` (`1`)
- `target` (`white_swatch`)

Attribute variant 11:
- `duration` (`1.5`) (optional)
- `ease` (`ease-in-out`) (optional)
- `name` (`_1T`) (optional)
- `opacity` (`.70`)
- `target` (`curtain2`)

Attribute variant 12:
- `auto` (`true`) (optional)
- `duration` (`1.5`) (optional)
- `ease` (`ease-in-out`) (optional)
- `name` (`_1h`) (optional)
- `opacity` (`0`)
- `target` (`curtain2`)

### IfEqual

Content variant 1:
- `events`
- `<class 'bs4.element.NavigableString'>`

Attribute variant 1:
- `lvalue` (`${slot:pick_count_albatros}`)
- `name` (`_5`)
- `rvalue` (`2`)

### CreditsEvent

Content variant 1:
- (Empty)

Attribute variant 1:
- `allowInterrupt` (`false`)
- `name` (`_4`)

### GameOver

Content variant 1:
- (Empty)

Attribute variant 1:
- `name` (`_5`)

### IfGTE

Content variant 1:
- `events`
- `<class 'bs4.element.NavigableString'>`

Attribute variant 1:
- `lvalue` (`${slot:pick_count_albatros}`)
- `name` (`_6`)
- `rvalue` (`3`)

Attribute variant 2:
- `auto` (`true`) (optional)
- `lvalue` (`${slot:pick_count_amazona}`)
- `name` (`_6`)
- `rvalue` (`3`)

### VarSet

Content variant 1:
- (Empty)

Attribute variant 1:
- `name` (`_0`) (optional)
- `storeName` (`game`) (optional)
- `value` (`true`)
- `varName` (`got_trueend_albatros`) (optional)

Attribute variant 2:
- `storeName` (`scene`) (optional)
- `value` (`true`)
- `varName` (`picked_once_albatros`) (optional)

Attribute variant 3:
- `auto` (`true`) (optional)
- `name` (`_6`) (optional)
- `storeName` (`slot`) (optional)
- `value` (`true`)
- `varName` (`picked_cousin_day1`) (optional)

Attribute variant 4:
- `name` (`got_trueend_galaga`) (optional)
- `value` (`true`)

### transitions

Content variant 1:
- `<class 'bs4.element.NavigableString'>`
- `opacity`

Content variant 2:
- `<class 'bs4.element.NavigableString'>`
- `x`

Content variant 3:
- `scaleY`
- `scaleX`
- `<class 'bs4.element.NavigableString'>`
- `y`

Attribute variant 1:
- (Empty)

### AudioOneShot

Content variant 1:
- (Empty)

Attribute variant 1:
- `auto` (`true`) (optional)
- `sound` (`@a_sfx_sentaisignal`)

Attribute variant 2:
- `sound` (`@a_sfx_sentaisignal`)

Attribute variant 3:
- `auto` (`true`) (optional)
- `name` (`_K`) (optional)
- `sound` (`@a_sfx_sentaisignal`)

Attribute variant 4:
- `name` (`_1`) (optional)
- `sound` (`@a_sfx_transform2`)

Attribute variant 5:
- `delay` (`1`) (optional)
- `sound` (`@a_sfx_sentaisignal`)

Attribute variant 6:
- `allowInterrupt` (`false`) (optional)
- `auto` (`true`) (optional)
- `name` (`_3`) (optional)
- `sound` (`@a_sfx_sentaisignal`)

Attribute variant 7:
- `delay` (`0.3`) (optional)
- `name` (`_2W`) (optional)
- `sound` (`@a_sfx_slam`)

### ActorDepth

Content variant 1:
- (Empty)

Attribute variant 1:
- `depth` (`0`)
- `target` (`king`)

Attribute variant 2:
- `auto` (`true`) (optional)
- `depth` (`1`)
- `target` (`curtain`)

Attribute variant 3:
- `auto` (`true`) (optional)
- `depth` (`10`)
- `name` (`_48`) (optional)
- `target` (`curtain`)

Attribute variant 4:
- `depth` (`15`)
- `name` (`_2`) (optional)
- `target` (`curtain`)

Attribute variant 5:
- `auto` (`true`) (optional)
- `delay` (`1`) (optional)
- `depth` (`10`)
- `name` (`_1h`) (optional)
- `target` (`curtain`)

### AudioEvent

Content variant 1:
- (Empty)

Attribute variant 1:
- `fadeDuration` (`1`)
- `name` (`_4`) (optional)
- `target` (`bgm`)
- `volume` (`0`)

Attribute variant 2:
- `fadeDuration` (`1`)
- `play` (`true`) (optional)
- `sound` (`@a_bgm_privatetimes`) (optional)
- `target` (`bgm`)
- `volume` (`1`)

Attribute variant 3:
- `fadeDuration` (`1`)
- `target` (`bgm`)
- `volume` (`0`)

Attribute variant 4:
- `fadeDuration` (`1`)
- `play` (`true`) (optional)
- `target` (`bgm`)
- `volume` (`1`)

Attribute variant 5:
- `allowInterrupt` (`false`) (optional)
- `delay` (`1`) (optional)
- `fadeDuration` (`1`)
- `target` (`river`)
- `volume` (`0`)

Attribute variant 6:
- `fadeDuration` (`1`)
- `name` (`_0`) (optional)
- `play` (`true`) (optional)
- `sound` (`@a_bgm_privatetimes`) (optional)
- `target` (`bgm`)
- `volume` (`1`)

### NormalEndMacro

Content variant 1:
- (Empty)

Attribute variant 1:
- `cousinX` (`-15%`) (optional)
- `name` (`_3`)
- `partnerActor` (`amazona`)
- `partnerImage` (`@i_amazona_akimbo_smile`)
- `partnerX` (`-35%`) (optional)

Attribute variant 2:
- `cousinX` (`-5%`) (optional)
- `name` (`_4`)
- `partnerActor` (`galaga`)
- `partnerImage` (`@i_galaga_default`)
- `runCredits` (`true`) (optional)

Attribute variant 3:
- `comparison` (`${slot:pick_count_nidia} == 2`) (optional)
- `name` (`_4`)
- `partnerActor` (`nidia`)
- `partnerImage` (`@i_nidia_resolute_happy`)
- `runCredits` (`true`) (optional)

Attribute variant 4:
- `cousinX` (`-5%`) (optional)
- `name` (`_3`)
- `partnerActor` (`miller`)
- `partnerImage` (`@i_miller_standing_laugh`)

### SuperSecretMacro

Content variant 1:
- (Empty)

Attribute variant 1:
- `name` (`_7`)

### ExplorationMacro

Content variant 1:
- (Empty)

Attribute variant 1:
- `lockout` (`true`)
- `name` (`day0.5choice`)
- `passText` (`@t_ui_noclubs`)
- `sceneName` (`day0.5`)

### Macro

Content variant 1:
- `events`
- `<class 'bs4.element.NavigableString'>`

Attribute variant 1:
- `name` (`albatros`)

Attribute variant 2:
- `auto` (`true`) (optional)
- `name` (`FirstTime`)

### IfFalse

Content variant 1:
- `events`
- `<class 'bs4.element.NavigableString'>`

Attribute variant 1:
- `auto` (`true`) (optional)
- `name` (`FirstTime`)
- `value` (`${scene:picked_once_albatros}`)

Attribute variant 2:
- `name` (`firstDialouge`)
- `value` (`${scene:picked_once_davesprite}`)

### LockoutChoice

Content variant 1:
- (Empty)

Attribute variant 1:
- `auto` (`true`)
- `character` (`albatros`)
- `name` (`_T`)
- `noText` (`t_salbatros1_choice3`)
- `promptText` (``) (optional)
- `storeName` (`slot`)
- `varName` (`picked_albatros_day1`)
- `yesText` (`t_salbatros1_choice2`)

Attribute variant 2:
- `auto` (`true`)
- `character` (`albatros`)
- `name` (`_2`)
- `noText` (`t_salbatros1_choice1`)
- `storeName` (`slot`)
- `varName` (`picked_albatros_day1`)
- `yesText` (`t_salbatros1_choice0`)

### IfTrue

Content variant 1:
- `events`
- `<class 'bs4.element.NavigableString'>`

Attribute variant 1:
- `auto` (`true`) (optional)
- `name` (`Loop`)
- `value` (`${scene:picked_once_albatros}`)

Attribute variant 2:
- `name` (`bail`)
- `value` (`${scene:doneExplore}`)

### BranchMacro

Content variant 1:
- (Empty)

Attribute variant 1:
- `comparison` (`!!${slot:picked_albatros_day1}`)
- `name` (`_2`)
- `path` (`albatros/Chosen`)

### JumpEvent

Content variant 1:
- (Empty)

Attribute variant 1:
- `name` (`_4`)
- `path` (`albatros/PassedFirstTime`)

### ConsoleEvent

Content variant 1:
- (Empty)

Attribute variant 1:
- `name` (`_0`)
- `value` (`HEY YOU PICKED AL COOL`)

Attribute variant 2:
- `name` (`_7`)
- `severity` (`error`) (optional)
- `value` (`Did not pick final branch for albatros ; failing!`)

### VarInc

Content variant 1:
- (Empty)

Attribute variant 1:
- `name` (`_6`)
- `storeName` (`slot`)
- `varName` (`pick_count_albatros`)

Attribute variant 2:
- `auto` (`true`) (optional)
- `name` (`_5`)
- `storeName` (`slot`)
- `varName` (`pick_count_cousin`)

### ActorReset

Content variant 1:
- (Empty)

Attribute variant 1:
- `target` (`cousin`)

### YesNoChoice

Content variant 1:
- (Empty)

Attribute variant 1:
- `auto` (`true`)
- `name` (`choice`)
- `noText` (`@t_scousin1_choice0`)
- `storeName` (`scene`)
- `varName` (`doneExplore`)
- `yesText` (`@t_scousin1_choice1`)

Attribute variant 2:
- `auto` (`true`)
- `name` (`_7`)
- `noText` (`@t_samazona5_choice1`)
- `promptText` (`@t_samazona5_prompt0`) (optional)
- `storeName` (`slot`)
- `varName` (`picked_amazona_day5`)
- `yesText` (`@t_samazona5_choice0`)

### HardSwap

Content variant 1:
- (Empty)

Attribute variant 1:
- `auto` (`true`) (optional)
- `lastActor` (`aki`)
- `name` (`_Z`)
- `nextActor` (`taira`)

Attribute variant 2:
- `lastActor` (`meowkie`)
- `name` (`_E`)
- `nextActor` (`aki`)

### ActorScale

Content variant 1:
- (Empty)

Attribute variant 1:
- `target` (`bike`)
- `x` (`1`)

Attribute variant 2:
- `name` (`_4`) (optional)
- `target` (`lolo`)
- `x` (`1`)

Attribute variant 3:
- `auto` (`true`) (optional)
- `target` (`cousin`)
- `x` (`0.75`)
- `y` (`0.75`) (optional)

Attribute variant 4:
- `delay` (`.4`) (optional)
- `target` (`cousin`)
- `x` (`-1`)

### SilhouetteMacro

Content variant 1:
- (Empty)

Attribute variant 1:
- `actorName` (`@t_ch_donko`)
- `actorScaleX` (`0.75`) (optional)
- `actorScaleY` (`0.75`) (optional)
- `actorY` (`12.5%`) (optional)
- `appendSecond` (`true`) (optional)
- `cousinActor` (`cousin`)
- `cousinScaleX` (`1`)
- `cousinX` (`-20%`)
- `firstText` (`@t_sintro21.00`)
- `name` (`_u`)
- `realActor` (`alt1`)
- `realImage` (`@i_donko_akimbo_wink`)
- `secondText` (`@t_sintro21.01`) (optional)
- `silActor` (`alt1_sil`)
- `silImage` (`@i_donko_akimbo_sil`)

Attribute variant 2:
- `actorName` (`@t_ch_mrdriller`)
- `appendSecond` (`false`) (optional)
- `cousinActor` (`cousin`)
- `cousinScaleX` (`-1`)
- `cousinX` (`20%`)
- `firstText` (`@t_sintro23.00`)
- `lastActor` (`alt1`) (optional)
- `name` (`_w`)
- `realActor` (`alt2`)
- `realImage` (`@i_mrdriller_slumped_happy`)
- `secondText` (`@t_sintro24.00`) (optional)
- `silActor` (`alt2_sil`)
- `silImage` (`@i_mrdriller_slumped_smug_sil`)

Attribute variant 3:
- `actorName` (`@t_ch_max`)
- `cousinActor` (`cousin`)
- `cousinScaleX` (`1`)
- `cousinX` (`-20%`)
- `firstText` (`@t_sintro28.00`)
- `lastActor` (`alt2`) (optional)
- `name` (`_z`)
- `realActor` (`alt1`)
- `realImage` (`@i_bluemax_cower_smile`)
- `silActor` (`alt1_sil`)
- `silImage` (`@i_bluemax_cower_sil`)

Attribute variant 4:
- `actorName` (`@t_ch_antibravo`)
- `actorScaleX` (`0.95`) (optional)
- `actorScaleY` (`0.95`) (optional)
- `actorY` (`2.5%`) (optional)
- `cousinActor` (`cousin`)
- `cousinScaleX` (`-1`)
- `cousinX` (`20%`)
- `firstText` (`@t_sintro31.00`)
- `lastActor` (`alt1`) (optional)
- `name` (`_12`)
- `realActor` (`alt2`)
- `realImage` (`@i_abm_backturned_broody`)
- `silActor` (`alt2_sil`)
- `silImage` (`@i_abm_backturned_sil`)

### id

Content variant 1:
- `<class 'bs4.element.NavigableString'>`

Attribute variant 1:
- (Empty)

### ActorTransform

Content variant 1:
- (Empty)

Attribute variant 1:
- `name` (`_3`)
- `scaleX` (`1`) (optional)
- `target` (`cousin`)
- `x` (`-20%`)

Attribute variant 2:
- `name` (`_0`)
- `target` (`cousin`)
- `x` (`-20%`)

